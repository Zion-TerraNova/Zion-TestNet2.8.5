import MDXPageLayout from "@/components/MDXPageLayout";

export default function Layout({ children }) {
  return <MDXPageLayout>{children}</MDXPageLayout>;
}

# Whitepaper Lite

A brief overview of consensus design and token economics for ZION TestNet.

## Consensus

High-level description of the consensus approach and validator incentives.

## Tokenomics

Emission schedule, distribution, and incentives. Details subject to change on TestNet.

> For the full paper, see legacy docs while we migrate.
