import MDXPageLayout from "@/components/MDXPageLayout";

export default function Layout({ children }) {
  return <MDXPageLayout>{children}</MDXPageLayout>;
}

# Pool Setup

Run a mining pool for ZION TestNet.

## Requirements

Hardware, OS, network, and dependencies.

## Configuration

Ports, stratum, payout logic, and monitoring.
